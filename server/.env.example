PORT=3005
JWT_SECRET=your-secret-key-change-in-production

# Environment: development | production
NODE_ENV=development

# Email Configuration (Aliyun DirectMail SMTP)
# Create sender address at: https://dm.console.aliyun.com/
EMAIL_USER=noreply@mail.yourdomain.com
EMAIL_APP_PASS=your-smtp-password

# Cloudflare R2 Storage
# Get these from Cloudflare Dashboard -> R2 -> Manage R2 API Tokens
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET=your-bucket-name
R2_PUBLIC_DOMAIN=files.yourdomain.com

# Firebase Push Notifications
# Get service account from: Firebase Console -> Project Settings -> Service Accounts -> Generate New Private Key
# Option 1 (Recommended): Place the downloaded JSON file as server/firebase-service-account.json
# Option 2: Set FIREBASE_SERVICE_ACCOUNT environment variable with the JSON content as a single line
